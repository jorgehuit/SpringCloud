#SPRING CONFIG
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jorgehuit/server-config

#SERVER CONFIG
server:
  port: 8888          
